# Secrets — NEVER commit these
api keys.txt
api-keys.txt
*.key
*.pem
.env
.env.*

# Vercel / tooling
.vercel/
node_modules/

# OS / editor cruft
Thumbs.db
.DS_Store

# Local-only: harness settings + the screenshot you dropped in
.claude/
Снимок экрана*.png
*.screenshot.png

.vercel
